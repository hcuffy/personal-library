<html>
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css">
    <link href="/main.css" rel="stylesheet" type="text/css">
    <title>Single Book</title>
  </head>
  <body>
    <div class="book-left">
      <span><%= book.title %></span><br>
      <% if(book.comments.length == 0){ %>
        <span>No Comments</span>
        <% }else{ %>
          <% for (var i = 0; i < book.comments.length; i++) { %>
            <span><%= book.comments[i] %></span><br>
            <% } %>
              <% } %>

    </div>
    <div class="book-right">
      <form class="comment-form">
        <div class="form-group">
          <label for="comment">Comment:</label>
          <textarea class="form-control" id="comment" rows="3" name="comment" placeholder="New Comments" required></textarea>
        </div>
        <button id="<%= book.id %>" type="button" class="btn btn-info comment-btn">Add Comment</button>
      </form>
      <form class="new-book-form" action="/single-book/" method="get">
        <div class="form-group">
          <label for="single-title"> Get a book:</label>
          <input class="form-control issue" id="single-title" type="text" name="id" placeholder="Book ID*" required>
        </div>
        <button type="submit" type="button" class="btn btn-secondary single-btn">Get Book</button>
      </form>
    </div>

    <a href="/" class="a_border"><button type="button" class="btn btn-secondary back-btn">&#x25C0; Back</button></a>
    <button type="button" class="btn btn-secondary multi-btn">All Books</button>
    <script src="https://code.jquery.com/jquery-3.3.1.min.js"></script>
    <script type="text/javascript" src="/main.js"></script>
  </body>

</html>
